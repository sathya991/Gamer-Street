import 'package:flutter/material.dart';

class InTournamentChat extends StatelessWidget {
  const InTournamentChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("This is the in tournament chat screen"),
      ),
    );
  }
}
