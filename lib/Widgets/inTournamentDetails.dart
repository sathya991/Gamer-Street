import 'package:flutter/material.dart';

class InTournamentDetails extends StatelessWidget {
  const InTournamentDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("This is the in tournament Screen"),
      ),
    );
  }
}
