import 'package:flutter/material.dart';

class Screenshots extends StatelessWidget {
  const Screenshots({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Screenshots page"),
      ),
    );
  }
}
