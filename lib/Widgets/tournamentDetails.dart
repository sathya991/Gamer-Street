import 'package:flutter/material.dart';

class AllTournamentDetails extends StatelessWidget {
  const AllTournamentDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("This is the tournament Detail Screen"),
      ),
    );
  }
}
