import 'package:flutter/material.dart';

class TourneySmallDisplay extends StatelessWidget {
  const TourneySmallDisplay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is a toruney"),
    );
  }
}
