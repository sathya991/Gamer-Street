import 'package:flutter/material.dart';

class RegistrationsScreen extends StatelessWidget {
  const RegistrationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is Registrations Screen"),
      ),
    );
  }
}
